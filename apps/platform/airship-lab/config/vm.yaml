apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: airship-lab
  namespace: airship-lab
spec:
  running: true
  template:
    metadata:
      labels:
        kubevirt.io/vm: airship-lab
    spec:
      nodeSelector:
        node-role/storage: "true"
      domain:
        cpu:
          cores: 4
          sockets: 1
          threads: 1
        memory:
          guest: 16Gi
        devices:
          disks:
            - name: rootdisk
              disk:
                bus: virtio
            - name: cloudinit
              disk:
                bus: virtio
          interfaces:
            - name: default
              masquerade: {}
        resources:
          requests:
            cpu: "4"
            memory: 16Gi
      networks:
        - name: default
          pod: {}
      volumes:
        - name: rootdisk
          dataVolume:
            name: airship-lab-rootdisk
        - name: cloudinit
          cloudInitNoCloud:
            secretRef:
              name: airship-lab-cloudinit
