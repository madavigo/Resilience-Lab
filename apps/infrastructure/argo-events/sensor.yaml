# Sensor -- watches EventBus for Gitea issue events and sends ntfy notifications.
# Two triggers:
#   notify-new-issue   — fires on action=opened  (new issue created)
#   notify-new-comment — fires on action=created (new comment on any issue)
# Each trigger uses `conditions` so they fire independently (OR semantics).
apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: gitea-issue-notify
  namespace: argo-events
spec:
  template:
    serviceAccountName: argo-events-sensor
  dependencies:
    - name: gitea-issue-webhook
      eventSourceName: emby-rifftrax
      eventName: gitea-issues
      filters:
        data:
          - path: body.action
            type: string
            value:
              - "opened"
    - name: gitea-comment-webhook
      eventSourceName: emby-rifftrax
      eventName: gitea-issues
      filters:
        data:
          - path: body.action
            type: string
            value:
              - "created"
  triggers:
    - template:
        name: notify-new-issue
        conditions: "gitea-issue-webhook"
        argoWorkflow:
          operation: submit
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: gitea-issue-notify-
                namespace: argo-events
              spec:
                workflowTemplateRef:
                  name: gitea-issue-notify
                arguments:
                  parameters:
                    - name: issue-title
                      value: placeholder
                    - name: issue-number
                      value: placeholder
                    - name: issue-url
                      value: placeholder
                    - name: issue-body
                      value: placeholder
                    - name: issue-user
                      value: placeholder
          parameters:
            - src:
                dependencyName: gitea-issue-webhook
                dataKey: body.issue.title
              dest: spec.arguments.parameters.0.value
            - src:
                dependencyName: gitea-issue-webhook
                dataKey: body.issue.number
              dest: spec.arguments.parameters.1.value
            - src:
                dependencyName: gitea-issue-webhook
                dataKey: body.issue.html_url
              dest: spec.arguments.parameters.2.value
            - src:
                dependencyName: gitea-issue-webhook
                dataKey: body.issue.body
              dest: spec.arguments.parameters.3.value
            - src:
                dependencyName: gitea-issue-webhook
                dataKey: body.issue.user.login
              dest: spec.arguments.parameters.4.value
      retryStrategy:
        steps: 3
        duration: 10s
    - template:
        name: notify-new-comment
        conditions: "gitea-comment-webhook"
        argoWorkflow:
          operation: submit
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: gitea-comment-notify-
                namespace: argo-events
              spec:
                workflowTemplateRef:
                  name: gitea-issue-notify
                arguments:
                  parameters:
                    - name: issue-title
                      value: placeholder
                    - name: issue-number
                      value: placeholder
                    - name: issue-url
                      value: placeholder
                    - name: issue-body
                      value: placeholder
                    - name: issue-user
                      value: placeholder
          parameters:
            - src:
                dependencyName: gitea-comment-webhook
                dataKey: body.issue.title
              dest: spec.arguments.parameters.0.value
            - src:
                dependencyName: gitea-comment-webhook
                dataKey: body.issue.number
              dest: spec.arguments.parameters.1.value
            - src:
                dependencyName: gitea-comment-webhook
                dataKey: body.comment.html_url
              dest: spec.arguments.parameters.2.value
            - src:
                dependencyName: gitea-comment-webhook
                dataKey: body.comment.body
              dest: spec.arguments.parameters.3.value
            - src:
                dependencyName: gitea-comment-webhook
                dataKey: body.comment.user.login
              dest: spec.arguments.parameters.4.value
      retryStrategy:
        steps: 3
        duration: 10s
---
# Sensor -- watches EventBus for emby-rifftrax events and submits workflow
apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: rifftrax-poster-sync
  namespace: argo-events
spec:
  template:
    serviceAccountName: argo-events-sensor
  dependencies:
    - name: emby-webhook
      eventSourceName: emby-rifftrax
      eventName: rifftrax-new-item
  triggers:
    - template:
        name: rifftrax-poster-sync-workflow
        argoWorkflow:
          operation: submit
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: rifftrax-poster-sync-
                namespace: argo-events
              spec:
                workflowTemplateRef:
                  name: rifftrax-poster-sync
      retryStrategy:
        steps: 3
        duration: 10s
