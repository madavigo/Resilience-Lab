# Per-node overlay for resilience-d05 — merged with worker-ephemeral-patch.yaml
machine:
  network:
    hostname: resilience-d05
    interfaces:
      - interface: enp0s31f6
        addresses:
          - 10.10.67.44/24
        routes:
          - network: 0.0.0.0/0
            gateway: 10.10.67.1
