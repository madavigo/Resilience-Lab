# Per-node overlay for resilience-d06 — merged with worker-ephemeral-patch.yaml
machine:
  network:
    hostname: resilience-d06
    interfaces:
      - interface: enp0s31f6
        addresses:
          - 10.10.67.45/24
        routes:
          - network: 0.0.0.0/0
            gateway: 10.10.67.1
